{
  "name": "db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "prisma db push",
    "dev": "bob-watch -c \"prisma generate\"",
    "db:generate": "prisma generate",
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:push": "prisma db push",
    "prepare": "touch ../../.env && ln -f ../../.env prisma/.env"
  },
  "dependencies": {
    "@prisma/client": "^3.2.1",
    "common-api": "workspace:^1.0.0",
    "prisma": "^3.2.1",
    "prisma-dbml-generator": "^0.8.1"
  },
  "devDependencies": {
    "@types/concurrently": "^6.3.0",
    "@types/react": "^17.0.30"
  }
}
