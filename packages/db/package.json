{
  "name": "db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "prisma db push",
    "dev": "bob-ts-watch -c \"prisma generate\"",
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:push": "prisma db push",
    "prepack": "bob-ts",
    "prepare": "touch ../../.env && ln -f ../../.env prisma/.env",
    "test:watch": "bob-ts-watch"
  },
  "dependencies": {
    "@prisma/client": "^3.1.1",
    "common-api": "workspace:^1.0.0",
    "prisma": "^3.1.1",
    "prisma-dbml-generator": "^0.7.0"
  },
  "devDependencies": {
    "@types/concurrently": "^6.2.1",
    "@types/react": "^17.0.24"
  }
}
